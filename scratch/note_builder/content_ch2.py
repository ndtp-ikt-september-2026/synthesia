# -*- coding: utf-8 -*-
"""
Глава 2: ПРОЕКТИРОВАНИЕ МОДУЛЯ РЕКОМЕНДАЦИЙ ДЛЯ «SYNTHESIA»
для пояснительной записки.
"""

from docx.shared import Pt, Mm, RGBColor
from .styles import add_p, add_h1, add_h2, add_formula, add_table, add_figure

def build_chapter_2(doc):
    add_h1(doc, "2 ПРОЕКТИРОВАНИЕ МОДУЛЯ ПЕРСОНАЛИЗИРОВАННЫХ РЕКОМЕНДАЦИЙ", page_break=True)

    add_h2(doc, "2.1 Общая структура и функциональная схема комплекса")

    add_p(doc, "Проектирование программного комплекса интернет-магазина «Synthesia» базируется на принципах сервисно-ориентированной модульной архитектуры с четким разделением контуров сбора данных, транзакционного ядра и машинного обучения. Это исключает влияние ресурсоемких вычислений векторов на время отклика витрины магазина.")

    add_p(doc, "Архитектурная схема программного комплекса представлена на Рисунке 2.1.")

    # Вставка Рисунка 2.1
    add_figure(doc, r"scratch\fig_arch.png", "Рисунок 2.1. – Архитектурная схема программного комплекса «Synthesia»", width_mm=160)

    add_p(doc, "Функциональная схема комплекса включает следующие ключевые подсистемы:")

    add_p(doc, "1. Клиентская витрина интернет-магазина: адаптивный интерфейс на базе HTML5/CSS3 и JavaScript ES6 с темной визуальной темой, интерактивным блоком треклистов и модальным окном рекомендаций.", first_indent=12.5)

    add_p(doc, "2. Транзакционное ядро OpenCart 3 LiveStore: контроллеры и модели на PHP, обеспечивающие управление каталогом, оформление заказов и сессионный контекст покупателя.", first_indent=12.5)

    add_p(doc, "3. Подсистема сбора и пакетного импорта: связка автономного скрипта synesthesia-parser (Python 3.13) и высокоскоростного обработчика cli/catalog_ingest.php (PHP CLI) для прямой записи в БД.", first_indent=12.5)

    add_p(doc, "4. Микросервис машинного обучения (synesthesia-ai): веб-сервис на FastAPI, рассчитывающий нормализованные центроиды релизов и выполняющий поиск семантического сходства в базе Qdrant.", first_indent=12.5)

    add_p(doc, "5. Сервер оперативного кэширования Redis: энергонезависимый слой кэширования ответов со временем отдачи до 1.5 мс.", first_indent=12.5)

    add_h2(doc, "2.2 Архитектура сбора данных synesthesia-parser")

    add_p(doc, "Для автоматизированного наполнения и актуализации ассортимента спроектирована подсистема synesthesia-parser, функционирующая по двум параллельным контурам:")

    add_p(doc, "1. Контур сбора музыкальных инструментов с pop-music.ru: обход разделов электрогитар, клавишных инструментов, синтезаторов и студийного оборудования с использованием curl_cffi для эмуляции сетевых отпечатков современных браузеров и обхода TLS-блокировок. Извлекаются артикулы, цены, фотографии и подробные таблицы параметров.", first_indent=12.5)

    add_p(doc, "2. Контур сбора релизов и треклистов через Discogs API: получение виниловых и компакт-дисковых изданий с контролем частоты обращений не более 60 запросов в минуту (DiscogsRateLimiter) и алгоритмической фильтрацией бутлегов и концертных записей filter_live_releases.", first_indent=12.5)

    add_p(doc, "3. Контур нормализации и векторизации: модуль AttributeNormalizer приводит параметры инструментов к атрибутам OpenCart, CategoryResolver определяет ветку каталога, а модуль InstrumentEncoder генерирует 384-мерный вектор признаков моделью MiniLM-L12-v2.", first_indent=12.5)

    add_h2(doc, "2.3 Модель данных и расширение схемы MySQL")

    add_p(doc, "Реляционная схема базы данных OpenCart была расширена двумя специализированными таблицами: oc_product_tracklist (хранение структурированных треклистов) и oc_product_vector_status (отслеживание состояния индексации в векторной базе данных).")

    add_p(doc, "Спецификация структуры полей таблицы треклистов представлена в Таблице 2.1.")

    headers_2_1 = ["Поле", "Тип данных", "NULL", "Ключ", "Назначение поля"]
    data_2_1 = [
        ["track_id", "INT(11)", "NO", "PRI", "Уникальный ключ дорожки (AUTO_INCREMENT)"],
        ["product_id", "INT(11)", "NO", "MUL", "Внешний ключ товара (oc_product)"],
        ["track_num", "INT(11)", "NO", "", "Порядковый номер композиции в релизе"],
        ["title", "VARCHAR(255)", "NO", "", "Наименование музыкальной композиции"],
        ["duration", "VARCHAR(32)", "NO", "", "Хронометраж в формате ММ:СС"],
        ["status", "TINYINT(1)", "NO", "", "Флаг видимости (1 - активен, 0 - скрыт)"]
    ]
    # Краткое название из 2-5 слов по Слайду 5
    add_table(doc, headers_2_1, data_2_1, col_widths=[30, 26, 20, 20, 80], title="Таблица 2.1 — Спецификация полей таблицы треклистов")

    add_p(doc, "Спецификация структуры полей таблицы векторного статуса представлена в Таблице 2.2.")

    headers_2_2 = ["Поле", "Тип данных", "NULL", "Ключ", "Назначение поля"]
    data_2_2 = [
        ["product_id", "INT(11)", "NO", "PRI", "Идентификатор товара в OpenCart"],
        ["is_indexed", "TINYINT(1)", "NO", "MUL", "Статус индексации в базе Qdrant"],
        ["has_audio", "TINYINT(1)", "NO", "", "Признак наличия дорожек релиза"],
        ["content_hash", "VARCHAR(32)", "NO", "", "Контрольная сумма MD5 контента товара"],
        ["updated_at", "TIMESTAMP", "NO", "", "Метка последней синхронизации"]
    ]
    add_table(doc, headers_2_2, data_2_2, col_widths=[30, 26, 20, 20, 80], title="Таблица 2.2 — Спецификация полей таблицы векторов")

    add_p(doc, "Логическая схема связей расширенных таблиц представлена на Рисунке 2.2.")

    # Вставка Рисунка 2.2
    add_figure(doc, r"scratch\fig_db.png", "Рисунок 2.2. – Логическая схема связей в базе данных", width_mm=160)

    add_h2(doc, "2.4 Математическая модель центроидных векторов")

    add_p(doc, "Математический аппарат модуля рекомендаций базируется на представлении каждого объекта в виде нормированного вектора размерности 384 в евклидовом пространстве признаков:")

    add_formula(doc, "v = (v_1, v_2, ..., v_{384}) in R^{384},   ||v||_2 = 1", "2.1")

    add_p(doc, "Для оценки степени семантической близости двух векторов u и v используется косинусная мера расстояния:")

    add_formula(doc, "CosineSim(u, v) = (u * v) / (||u|| * ||v||) = sum_{i=1}^{384} (u_i * v_i)", "2.2")

    add_p(doc, "Для сопоставления музыкального релиза, включающего m треков с индивидуальными векторами признаков v_k, с оборудованием каталога разработан алгоритм расчета нормализованного центроидного вектора альбома:")

    add_formula(doc, "c = ( sum_{k=1}^m v_k ) / || sum_{k=1}^m v_k ||_2", "2.3")

    add_p(doc, "Полученный центроидный вектор c передается в индекс HNSW базы Qdrant, возвращающий k наиболее близких инструментов с аппаратной фильтрацией по диапазону цен и наличию на складе.")
