# -*- coding: utf-8 -*-
"""
Глава 3: ПРОГРАММНАЯ РЕАЛИЗАЦИЯ МОДУЛЕЙ КОМПЛЕКСА
для пояснительной записки «Разработка модуля персонализированных рекомендаций на основе машинного обучения».
"""

from docx.shared import Pt, Mm, RGBColor
from .styles import add_p, add_h1, add_h2, add_figure

def build_chapter_3(doc):
    add_h1(doc, "3 ПРОГРАММНАЯ РЕАЛИЗАЦИЯ МОДУЛЯ И ИНТЕРНЕТ-МАГАЗИНА", page_break=True)

    add_h2(doc, "3.1 Разработка подсистемы сбора данных")

    add_p(doc, "Подсистема автоматизированного сбора данных synesthesia-parser реализована на языке Python 3.13 в виде набора специализированных модулей:")

    add_p(doc, "1. Модуль ScrapingEngine (scraper/gear_engine.py): осуществляет автоматизированный сбор каталога pop-music.ru через библиотеку curl_cffi с имитацией браузерных заголовков и параметров рукопожатия TLS, извлекая цены, характеристики и фотоматериалы.", first_indent=12.5)

    add_p(doc, "2. Модуль DiscogsClient (scraper/discogs_client.py): реализует клиент API музыкальной базы Discogs с контролем частоты запросов не более 60 в минуту (DiscogsRateLimiter) и алгоритмической фильтрацией бутлегов и концертных записей.", first_indent=12.5)

    add_p(doc, "3. Модуль AttributeNormalizer (scraper/normalizer.py): нормализует разрозненные строковые характеристики инструментов (тип звукоснимателей, материал деки, полифония) в канонические атрибуты интернет-магазина.", first_indent=12.5)

    add_p(doc, "4. Модуль CategoryResolver (scraper/category_resolver.py): эвристический классификатор, распределяющий инструменты по веткам каталога OpenCart LiveStore: «Гитары», «Гитарное оборудование», «Клавишные» и «Струнные».", first_indent=12.5)

    add_p(doc, "5. Модуль InstrumentEncoder (scraper/encoder.py): векторизует нормализованные описания с использованием модели MiniLM-L12-v2 в единичные 384-мерные векторы.", first_indent=12.5)

    add_h2(doc, "3.2 Сервис векторного кодирования и поиска FastAPI")

    add_p(doc, "Микросервис машинного обучения разработан на базе асинхронного фреймворка FastAPI (Python 3.13). В файле synesthesia-ai/app/main.py в контекстном менеджере lifespan инициализируется подключение к базе данных Qdrant и создается векторная коллекция с косинусной мерой расстояния Distance.COSINE.")

    add_p(doc, "В модуле synesthesia-ai/app/api/internal_v1.py реализованы следующие программные эндпоинты:")

    add_p(doc, "1. Маршрут GET /internal/v1/tracks/{track_id}/similar: производит поиск k семантически похожих композиций. Сначала опрашивается оперативный кэш Redis. При промахе выполняется асинхронный поиск ближайших соседей по индексу HNSW в Qdrant с фильтрацией по жанру.", first_indent=12.5)

    add_p(doc, "2. Маршрут GET /internal/v1/tracks/{track_id}/instruments: выполняет сквозной подбор музыкальных инструментов к выбранному треку, извлекая вектор композиции и сопоставляя его с векторами каталога оборудования.", first_indent=12.5)

    add_p(doc, "3. Маршрут POST /internal/v1/albums/similar-from-tracks: агрегирует векторы треков альбома, вычисляет нормализованный центроидный вектор функцией compute_centroid_vector и осуществляет поиск похожих музыкальных изданий.", first_indent=12.5)

    add_h2(doc, "3.3 Модуль структурированного треклиста OpenCart")

    add_p(doc, "Для карточек музыкальных релизов интернет-магазина «Synthesia» разработан модуль структурированного треклиста soundnet_tracklist. Модуль организует отображение состава альбома, номеров треков и их длительности без необходимости тяжелого медиа-хостинга на сервере.")

    add_p(doc, "Каталожный контроллер catalog/controller/extension/module/soundnet_tracklist.php считывает product_id, вызывает метод getTracks модели и передает упорядоченный массив в шаблон карточки товара. Административный контроллер admin/controller/extension/module/soundnet_tracklist.php добавляет вкладку управления треклистом в интерфейс каталога.")

    add_h2(doc, "3.4 Пользовательский интерфейс витрины «Synthesia»")

    add_p(doc, "Интерфейс витрины выполнен в современной темной стилистике с акцентными визуальными карточками и эффектами скругления элементов.")

    add_p(doc, "Внешний вид пользовательской витрины интернет-магазина «Synthesia» с интерактивным блоком персональных рекомендаций представлен на Рисунке 3.1.")

    # Вставка Рисунка 3.1
    add_figure(doc, r"scratch\live_storefront_rounded.png", "Рисунок 3.1. – Витрина интернет-магазина «Synthesia» с блоком рекомендаций", width_mm=155)

    add_p(doc, "Интерфейс включает блоки тематических подборок (шаблон soundnet_recommendations.twig), отображение формата издания на виниле или компакт-диске, а также модальное окно персональных рекомендаций со списком семантически подобранных синтезаторов и гитар.")

    add_h2(doc, "3.5 Пакетный импорт данных и подсистема StopKran")

    add_p(doc, "Для загрузки результатов работы парсера разработан высокопроизводительный консольный скрипт cli/catalog_ingest.php. Скрипт принимает подготовленные JSON-пакеты, атомарно обновляет таблицы oc_product, oc_product_attribute и oc_product_tracklist, пересчитывает цены в белорусские рубли по фиксированному курсу и фиксирует статус индексации в таблице oc_product_vector_status.")

    add_p(doc, "Для защиты ядра OpenCart от фатальных ошибок внедрена подсистема SoundNet\\StopKran: класс Watchdog (system/library/stopkran/watchdog.php) перехватывает критические сбои через register_shutdown_function, класс Recovery отключает сбойные модификации, а утилита cli/stopkran.php позволяет восстановить работу магазина за 0.18 секунды через прямое подключение PDO.")
